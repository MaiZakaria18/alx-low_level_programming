#include "main.h"
/**
 *_abs: Computes the absolute value of an integer.
 *
 * @param number The integer for which to calculate the absolute value.
 * @return The absolute value of the input integer.
 */
int _abs(int number)
{
if (number < 0)
{
number = -number;
}
return (number);
}
